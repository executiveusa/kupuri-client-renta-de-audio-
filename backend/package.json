{
  "name": "dos2a-backend",
  "version": "1.0.0",
  "private": true,
  "description": "DOS2A - Backend API Server",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "lint": "eslint api/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.13.0",
    "@sendgrid/mail": "^7.7.0",
    "@upstash/redis": "^1.25.0",
    "@vercel/node": "^3.0.0",
    "twilio": "^4.1.0",
    "zod": "^3.22.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.50.0"
  }
}
